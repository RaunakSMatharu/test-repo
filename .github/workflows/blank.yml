name: use-local-secret
on:
  workflow_dispatch:
    inputs: 
      USER_SECRET:
        description: "secret_value"
        required: true
        

jobs:
  secure-job:
    runs-on: self-hosted
    steps:
      - name: mask secret
        shell: bash
        run: |
          val=${{ inputs.USER_SECRET }}
          echo "::add-mask::$val"
